# Ensure shell scripts always use LF (Unix) line endings
*.sh text eol=lf

# Ensure Dockerfile uses LF line endings  
Dockerfile text eol=lf

# Let Git handle line endings for text files automatically
* text=auto

